/** Hard caps for Arknights: Endfield builds — enforced by the doc schema and inputs. */
export const ENDFIELD_LIMITS = {
  maxLevel: 80,
  maxSkill: 10,
  maxPotential: 6,
  maxWeaponLevel: 80,
  maxGearLevel: 20,
} as const;
