/** Hard caps for Genshin Impact builds — enforced by the doc schema and inputs. */
export const GENSHIN_LIMITS = {
  maxLevel: 90,
  maxTalent: 10,
  maxConstellation: 6,
  maxWeaponLevel: 90,
  maxRefinement: 5,
  maxArtifactLevel: 20,
} as const;
